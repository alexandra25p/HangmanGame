<Window x:Class="HangmanGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HangmanGame.ViewModels"
        mc:Ignorable="d"
        Title="Sign In" Height="550" Width="800" WindowStartupLocation="CenterScreen"
        Background="#F4F0FA">

    <Window.DataContext>
        <vm:StartViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4A306D"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D4C4F0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="#4A306D"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D4C4F0"/>
                    <Setter Property="Foreground" Value="#4A306D"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F4F0FA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#D4C4F0"/>
            <Setter Property="Foreground" Value="#4A306D"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#BFA8E5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#BFA8E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to Hangman! Enjoy this game!" 
                   FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" 
                     ItemsSource="{Binding Users}" 
                     SelectedItem="{Binding SelectedUser, Mode=TwoWay}" 
                     DisplayMemberPath="Username"
                     Margin="0,0,15,0" FontSize="18"/>

            <Border Grid.Column="1" Margin="15,0,0,0" BorderBrush="#D4C4F0" BorderThickness="3" CornerRadius="15" Background="White">
                <Image Source="{Binding SelectedUser.FullImagePath}" Stretch="Uniform" Margin="10">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedUser}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
            <Button Content="New User" Command="{Binding NewUserCommand}" 
                    Width="120" Height="40" Margin="10,0"/>

            <Button Content="Delete User" Command="{Binding DeleteUserCommand}" 
                    Width="120" Height="40" Margin="10,0"/>

            <Button Content="Play" Command="{Binding PlayCommand}" 
                    Width="120" Height="40" Margin="10,0" Background="#BFA8E5"/>

            <Button Content="Cancel" Click="Cancel_Click" 
                    Width="120" Height="40" Margin="10,0" Background="#EADDF8"/>
        </StackPanel>
    </Grid>
</Window>