<Window x:Class="HangmanGame.View.LoadGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Load Game" Height="400" Width="400"
        WindowStartupLocation="CenterScreen" Background="#F4F0FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Pick a saving:" FontSize="18" FontWeight="Bold" Foreground="#4A306D" Margin="0,0,0,10"/>

        <ListBox x:Name="SavesListBox" Grid.Row="1" Background="White" BorderBrush="#D4C4F0" BorderThickness="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="14" Foreground="#4A306D"/>
                        <TextBlock Text="{Binding SaveDate}" FontSize="12" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Load" Width="80" Height="30" Margin="0,0,10,0" Background="#EADDF8" Click="Load_Click"/>
            <Button Content="Cancel" Width="80" Height="30" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>