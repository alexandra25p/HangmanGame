<Window x:Class="HangmanGame.View.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detailed Statistics" Height="520" Width="680"
        WindowStartupLocation="CenterScreen" Background="#F4F4F9">

    <Window.Resources>
        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4B2C5E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Players Statistics" FontSize="24" FontWeight="Bold" 
                   Foreground="#4B2C5E" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="#F4F0FA" CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <ComboBox x:Name="CategoryFilterCombo" Width="140" Height="28" Margin="0,0,15,0"
                          SelectionChanged="CategoryFilter_Changed" VerticalContentAlignment="Center"/>

                <Button Content="My Profile" Style="{StaticResource FlatButtonStyle}" 
                        Click="MyStats_Click" Background="#85739C" Width="110" Margin="0,0,10,0"/>

                <Button Content="Reset" Style="{StaticResource FlatButtonStyle}" 
                        Click="ShowAll_Click" Background="#D4C4F0" Width="70"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="StatsDataGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" CanUserAddRows="False" 
                  Background="#F4F0FA" BorderBrush="#DDD" RowHeight="32"
                  AlternatingRowBackground="#F4F0FA" GridLinesVisibility="Horizontal" 
                  HorizontalGridLinesBrush="#EEE" SelectionChanged="StatsDataGrid_SelectionChanged">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Player" Binding="{Binding User}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="1.3*"/>
                <DataGridTextColumn Header="Played" Binding="{Binding Played}" Width="80"/>
                <DataGridTextColumn Header="Won" Binding="{Binding Won}" Width="80"/>
                <DataGridTextColumn Header="Success (%)" Binding="{Binding WinPercent, StringFormat={}{0:F1}%}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="#2E7D32"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Back To The Game" Style="{StaticResource FlatButtonStyle}"
                Click="Close_Click" Width="130" Height="30" Margin="0,15,0,0" HorizontalAlignment="Center"/>
    </Grid>
</Window>